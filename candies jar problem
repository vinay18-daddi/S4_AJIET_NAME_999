public static void main(String[] args) {

       Scanner sc = new Scanner(System.in);

     
       int N = sc.nextInt();
       int f = sc.nextInt(); // candies that customer wants
       int min = sc.nextInt(); // minimum candies limit

       int total = N;
       int count = 0;

       if (f>N)

           System.out.println("cannote store all the candies");

       if (f <= N)
       {
           total = total - f;
           count = count + f;

           System.out.println("Number of candies sold:" + f);
       }

       if (total <= min)
       {
           total = N;
       }

       System.out.println("Number of candies remaining:" + total);

       sc.close();
    }
}
